// Configuration settings for the application
// After deploying the server to Render, replace the default URL with your actual backend URL.

const CONFIG = {
    API_URL: import.meta.env.VITE_API_URL,
};

export default CONFIG;
